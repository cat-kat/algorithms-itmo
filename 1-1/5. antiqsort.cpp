#include <iostream>
#include <fstream>
using namespace std;
 
int main() {
 int n;
  ifstream fin("antiqs.in"); 
  fin>>n;
    fin.close();
 int *mas = new int[n];
 for (int i=0; i<n; i++) {
    mas[i]=i+1;
 }
  
  for (int i=0; i<n; i++) {
      
      
        swap(mas[i], mas[i / 2]);
    }
         
         ofstream fout("antiqs.out"); // ??????? ?????? ?????? ofstream ??? ?????? ? ????????? ??? ? ?????? cppstudio.txt
    for (int i=0; i<n; i++) {
    fout<<mas[i]<<" ";
 }
    fout.close(); // ????????? ????
}